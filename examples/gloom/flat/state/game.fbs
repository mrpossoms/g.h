include "player.fbs";
include "world.fbs";

namespace gloom.state;

enum State:byte { unknown = 0, waiting, ready, playing }

table game
{
	world: world.Info;
	state: State;

	my_id: uint32;
	players:[Player];
	player_configs:[PlayerConfig];

	chunk_delta:[world.Delta];
}