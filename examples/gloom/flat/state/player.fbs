include "../types.fbs";

namespace gloom.api.state;

struct Player {
	id: uint32;
	position: Vec3f;
	velocity: Vec3f;
	orientation: Quat;
}

table PlayerConfig {
	id: uint32;
	name: string;
}